---
description: "Consistency + quality verdict. Advisory after_analyze hook + mandatory after_implement hook. Computes requirement-level coverage (Strong/Medium/Weak/Stub/Missing), runs stub + traceability + constitution + checklist checks, lifts /speckit-analyze findings, emits Gate: PASS / BLOCKED, and writes FEATURE_DIR/review.md (overwritten each run). The after_implement run is the formal pre-merge gate."
argument-hint: "[--strict] [--story US1] [--no-write]"
---

# Consistency + Quality Verdict

Runs on `after_analyze` (advisory pre-impl read) and `after_implement` (formal pre-merge gate, with real test results).

**Writes:** `FEATURE_DIR/review.md` only (overwritten each run). All other artefacts read-only.
**Source of truth for consistency:** `/speckit-analyze`'s report (lifted from conversation; not re-derived).

## User Input

```text
$ARGUMENTS
```

Flags: `--strict` (Majors → Blockers), `--story US1`, `--no-write` (chat-only; tracking is the default).

## Prerequisites

1. `.specify/scripts/powershell/check-prerequisites.ps1 -Json -RequireTasks -IncludeTasks` → parse `FEATURE_DIR`.
2. Read `spec.md`, `plan.md` (incl. `## Testing Strategy` — **read `Change Profile:` tags from the header**), `tasks.md`, `test-plan.md`, `checklists/test.md`, `.specify/memory/constitution.md`. Scan `tests/`.
3. **Lift `/speckit-analyze`'s in-conversation report** when available: finding table (A1, D1…), Coverage Summary, Unmapped Tasks, Constitution Alignment.

## Checks

### 1. Requirement-level coverage

For every gated item (P1 `US{n}-AS{m}`, `FR-###`, buildable `SC-###`), rate the mapping:

| Rating | Criteria |
|---|---|
| **Strong** | ≥1 non-stub test names the item ID + exercises documented behaviour |
| **Medium** | Test names the item; partial coverage (happy path only) |
| **Weak** | Test in the right area but no item ID — traceability broken |
| **Stub** | Only the qaprep failing stub exists — 0% coverage |
| **Missing** | No test file |

Constitution threshold decides which ratings escalate to Blocker (default: every P1 → Strong; SC perf may be Medium).

### 2. Stub scan
Unchanged `qaprep` failing stub + P1 item → **Blocker**.
Body matches "stub by description" task → **Major**.

### 3. Cross-reference `/speckit-analyze`
- CRITICAL → Blocker (prefix `[ANALYZE]`, preserve analyze ID).
- Constitution-aligned violation → Blocker.
- Requirement with no mapped task (per analyze) → Blocker.
- Unmapped task (per analyze) → Major (scope drift).

### 4. Traceability chain
Verify end-to-end: `spec item → plan.md Testing Strategy row → tasks.md test task → test file with ID in header → test-plan.md matrix row`. Broken link → Major.

### 5. Artefact presence
- `test-plan.md` exists + every gated item in matrix + every P1 item has ≥1 TC in §3 Catalogue → else Blocker.
- `checklists/test.md` exists + gated rows ticked **with non-empty Evidence** → ticked-without-evidence is Major (Blocker under `--strict`).
- Constitution-required test-plan sections present → else Major.

### 6. Execution evidence (after_implement only)
- Every P1 TC (auto + manual) has ≥1 entry in `test-plan.md` §8 Test Execution Log naming a build/commit ≥ current head → else Blocker.
- Any P1 TC last-run = FAIL with no linked defect ID → Blocker.
- Flaky TCs (mixed pass/fail in last 3 runs) → Major.
- On `after_analyze` (no real runs yet) skip Check 6; flag Catalogue completeness only.

### 7. Change-Profile checks (per active tag)
- `bugfix` — reproducing test must exist + have a *failing* run pre-fix and a *passing* run post-fix in §8; else Blocker.
- `refactor` — characterization suite is green; **no new functional cases** added vs prior run unless plan declares a new public-surface entry; new functional cases without surface change = Major (scope drift).
- `concurrency` — stress test ran ≥ N (constitution-declared, default 100) iterations with zero deadlock/data-race report; else Blocker.
- `performance` — perf run within budget vs baseline file; over-budget regression = Blocker (perf SC rated Strong only when within budget).
- `security` — SAST + dep-scan attached to §8 with zero new High/Critical; authz-negative tests green; else Blocker.
- `ui` — visual diff reviewed (Evidence: diff URL); cross-browser matrix green; outstanding diff without sign-off = Major.
- `api` — schema-compat report: breaking change without version bump = Blocker; non-breaking but undocumented = Major.
- `data-migration` — up/down/rollback rehearsed on staging with §8 evidence; partial backfill = Blocker.

Missing `Change Profile` header in plan.md → Major (the feature predates planaudit's profile detection or planaudit was skipped).

## Output — `review.md` (and chat)

Same body in both; file gets a metadata header prepended.

```markdown
<!-- Generated by speckit.test.qareview. Overwritten each run. -->

# Consistency + Quality Verdict — <feature>

| Field | Value |
|---|---|
| Feature | specs/001-… |
| Run at | 2026-06-08T14:32:11Z |
| Triggered by | after_analyze (advisory) / after_implement (formal) |
| Verdict | **Gate: BLOCKED ❌** or **Gate: PASS ✅** |
| Mode | normal / --strict / --no-write |

Change Profile: [api, security]
Gated items: 12 (8 P1, 4 SC) | Strong: 7 | Medium: 2 | Weak: 1 | Stub: 1 | Missing: 1
TC Catalogue: 18 (14 auto, 4 manual) | Last run: 14 pass, 2 fail, 2 not-run | Defects open: 1 (BUG-417)
Profile checks: schema-compat=pass, authz-negative=pass, SAST=0 high, dep-scan=0 high

### ❌ Blockers
| ID | Where | Issue | Action |
|----|-------|-------|--------|
| B1 | US1-AS2 | Integration stub still failing | Implement tests/integration/filter-persistence.integration.test.ts |
| B2 | FR-007 | No test of any kind | Add unit + integration |
| B3 | [ANALYZE] A4 | Constitution MUST: SC-001 perf untested | Add k6 scenario |

### ⚠️ Majors
| ID | Where | Issue |
|----|-------|-------|
| M1 | tests/integration/checkout.test.ts | No US3-AS1 reference (weak) |
| M2 | T021 | Unmapped to any spec item (drift) |

### ℹ️ Minors
| ID | Where | Issue |
|----|-------|-------|
| m1 | test-plan.md §Risk | 2 rows vs 4 edge cases — extend |

### Constitution
- "Integration Testing": 1 violation (B3)
- "Test-First": clean (planaudit + tasksaudit + qaprep ran)

### Checklist
checklists/test.md: 12 of 15 ticked; 3 unchecked (rows 7, 9, 13)

### Verdict
**Gate: BLOCKED ❌** — 3 Blockers must be resolved before merge.

SPECTEST QAREVIEW: 12 items, 7 Strong, 3 Blockers, 2 Majors, 1 Minor — FAIL
```

Clean:
```
### Verdict
**Gate: PASS ✅** — all gated items Strong; no Blockers; no violations.

SPECTEST QAREVIEW: 12 items, 12 Strong, 0 Blockers — PASS
```

Under `--strict`, Majors count as Blockers in the verdict line.

## Rules

- **Single write target: `review.md`** — overwritten each run; chat body and file body identical (file adds metadata header). `--no-write` skips file (rare).
- **Overwrite, not append** — history lives in git; file is the *latest* state.
- **Lift, don't re-derive, consistency findings** — `/speckit-analyze` is the source of truth.
- **`after_implement` is the formal gate** — it has real test results; `after_analyze` runs advisory.
- **Stubs ≠ coverage** — unchanged qaprep stub = `Stub = 0%`.
- **Traceability needs the item ID *in the test*** — without it, max rating is Weak.
- **Constitution MUSTs always escalate to Blocker** — cannot be silenced.
- **Missing test-plan.md / checklist / scaffolds → first Blocker is "re-run after-tasks (qaprep)".**
- **Checklist ticks need Evidence** — bare ticks don't count as sign-off.
- **`after_implement` requires Execution Log entries** — verdict cannot be PASS without recorded runs for every P1 TC.
- **Explicit verdict** — always `Gate: PASS|BLOCKED` + `SPECTEST QAREVIEW:` one-liner.
